import { List, X, Basket, Info, WarningCircle } from "@phosphor-icons/react";

const CoreIcons = {
    List: List,
    X: X,
    Basket: Basket,
    Info: Info,
    WarningCircle: WarningCircle,
};

export default CoreIcons;
